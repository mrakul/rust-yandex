[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "criterion"
harness = false

# Релизная сборка для perf'а/flamegraph с символами
[profile.release]
debug = 1