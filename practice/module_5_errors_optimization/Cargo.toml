[package]
name = "broken-app"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "baseline"
harness = false

# Релизная сборка для perf'а/flamegraph с символами
[profile.release]
debug = 1