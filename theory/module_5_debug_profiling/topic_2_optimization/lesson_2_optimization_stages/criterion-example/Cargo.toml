[package]
name = "criterion-example"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "my_bench"
# Не использовать встроенный test фреймворк
harness = false 
